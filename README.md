# grammar-knn-lm
A 530K-parameter language model that separates grammar from knowledge using link-grammar connector embeddings and kNN datastore retrieval. No hallucination, instant  knowledge updates, trainable on CPU.
